{
  "name": "rosas-monitor-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd dashboard && npm install && npm run build",
    "vercel-build": "cd dashboard && npm install && npm run build"
  }
}
